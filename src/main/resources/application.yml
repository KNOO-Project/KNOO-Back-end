jwt:
  secret: KNOO_SECRET_KEY_BASE_64

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/knoo?characterEncoding=UTF-8&serverTimezone=UTC
    username: jcw
    password: 1234
  jpa:
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace
    org.apache.coyote.http11: debug

server:
  port: 8888